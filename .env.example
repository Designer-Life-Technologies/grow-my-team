# Next Auth
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# GetMe.video API
GETME_API_URL=https://api.getme.video
# Optional host-based overrides (comma-separated host=baseUrl pairs)
# Example below maps two hosts to different tenant API bases
GETME_API_URL_MAP=careers.example.com=https://api.acme.getme.video,partners.example.com=https://api.beta.getme.video

# Logging
# Set to "false" to disable console logs (default: enabled in development, disabled in production)
NEXT_PUBLIC_ENABLE_LOGGING=true
